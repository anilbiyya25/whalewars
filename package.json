{
  "name": "whalewars",
  "version": "0.1.0",
  "private": true,
  "description": "Whale Wars — server-authoritative multiplayer whale tug-of-war",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.5",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
